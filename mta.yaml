_schema-version: 3.3.0
ID: NotaFiscalCAP
description: A simple CAP project.
version: 1.0.0
modules:
- name: NotaFiscalCAP-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: NotaFiscalCAP-auth
  - name: NotaFiscalCAP-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: NotaFiscalCAP-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: NotaFiscalCAP-db
  parameters:
    buildpack: nodejs_buildpack
- name: NotaFiscalCAP-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: NotaFiscalCAP-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - monitoramentomonitor.zip
      name: monitoramentomonitor
      target-path: resources/
    - artifacts:
      - configuracaoissui.zip
      name: configuracaoissui
      target-path: resources/
- name: monitoramentomonitor
  type: html5
  path: app/monitor
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: configuracaoissui
  type: html5
  path: app/configuracaoiss_ui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: NotaFiscalCAP-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: NotaFiscalCAP-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: NotaFiscalCAP-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: NotaFiscalCAP-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: NotaFiscalCAP-html5-service
    service-plan: app-host
- name: NotaFiscalCAP-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: NotaFiscalCAP-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
